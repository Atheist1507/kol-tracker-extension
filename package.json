{
  "name": "kol-tracker-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension tra cứu KOL/caller crypto ngay trên chart GMGN, dữ liệu từ Google Sheet (CSV)",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "icons": "node scripts/gen-icons.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
